# CLAUDE.md — solar-soiling-ml-public

The **public surface** of SolarSoiled: a curated snapshot of the pipeline. The homeowner dashboard
+ calculator now live in the **BBF site** (`betterbehaviorfoundation.com/tools/`); the GitHub Pages
landing/dashboard once hosted here has been retired. The full pipeline, training data, and any
parcel/PII data live in the private repo `solar-soiling-ml`. This repo is public — **nothing
PII-bearing or secret ships here**; `SYNC.md` is the pre-publish safety gate, run it before
publishing synced content.

> **Session start:** before re-investigating, read `SESSION_STARTUP.md` (repo root, gitignored) for
> current working state — env facts, the publishing gate, next actions, and cross-repo context.
> Keep it live and pruned as work progresses (see its "Maintenance cadence").
