# Core numerical and data handling
numpy>=1.23
pandas>=1.5

# Geospatial stack
rasterio>=1.3
geopandas>=0.13
shapely>=2.0
pyproj>=3.5
geojson>=3.0

# Image handling
Pillow>=9.4
scikit-image>=0.20

# Machine learning / modeling
torch>=2.0
ultralytics>=11.0
scikit-learn>=1.2
xgboost>=1.7

# Optional: GeoAI for visualization, STAC search, and SAM pre‑labeling
#   Preferred installation via conda-forge:  `conda install -c conda-forge geoai`
#   or with pip: `pip install geoai-py`
geoai-py

# Dataset management / labeling
roboflow>=1.0

# Small-object inference boost
sahi>=0.11

# Stage 2 — soiling risk model
requests-cache>=1.1
PyYAML>=6.0

# Backbone ablation (scripts/03c_train_lightning.py)
pytorch-lightning>=2.0
timm>=0.9
torchmetrics>=1.0

# Interactive map output (solarsoiled viz)
folium>=0.14

# SAM zero-shot comparison (scripts/05f_sam_comparison.py)
# Checkpoint download: see script header for wget commands
# Install: pip install git+https://github.com/facebookresearch/segment-anything.git
# segment-anything
